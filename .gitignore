# Secrets — never commit (this site is served as static files publicly)
.env
.env.*
*.env
.env.txt
# Credential / API-info drops (e.g. Google Cloud OAuth client secret)
*API INFO*.txt
*api info*.txt
GOOGLE CLOUD*.txt
grainger.txt

# Local build helper
.us3validate.sh

# OS / editor cruft
.DS_Store
Thumbs.db
.wrangler/
desktop.ini
